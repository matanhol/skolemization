__pycache__/
.idea/
.claude/settings.local.json
.DS_Store

# Generated: python3 build_notebook.py rebuilds these byte for byte from the
# package and the examples -- one per language -- so they are not worth a
# 60-cell JSON diff per edit.
skolemization.ipynb
skolemization.*.ipynb

# Working material from settling the right-to-left layout: screenshots of how
# the terminal actually renders Hebrew, and the scratch scripts that pinned
# the arithmetic down.  What was learned from them is in CLAUDE.md, which is
# the part worth keeping; the files themselves are not the model.
*.png
hebrew_*.py
