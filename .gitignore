__pycache__/
.idea/
.claude/settings.local.json
.DS_Store

# Generated: python3 build_notebook.py rebuilds these byte for byte from the
# package and the examples -- one per language -- so they are not worth a
# 60-cell JSON diff per edit.
skolemization.ipynb
skolemization.*.ipynb

# The author's screenshots of how the terminal actually renders Hebrew --
# working material, and megabytes of it.  hebrew_indentation.py is the part
# worth keeping, and it is tracked.
*.png
