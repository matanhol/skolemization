"""Worked examples, one package per problem.

Each problem folder keeps its assumptions in one module and gives every
conclusion its own script, so the same knowledge base can be interrogated
from several angles without being retyped::

    python3 -m examples.dogs.betrayer_not_dog
    python3 -m examples.uniqueness.with_eq_axioms
    python3 -m examples.equivalence.equivalence
    python3 -m examples.question8.pr_and_not_l

Run them from the repository root.  Nothing runs on import.
"""
